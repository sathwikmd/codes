void reverseString(char* s, int sSize) {
    int ptr1 = 0, ptr2 = sSize-1;
    while(ptr1 < ptr2){
        char temp = s[ptr1];
        s[ptr1] = s[ptr2];
        s[ptr2] = temp;
        ptr1++;
        ptr2--;
    }
}
